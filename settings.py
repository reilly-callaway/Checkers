def Set():
	#Design similar to menu program but going up/down
	#Hit enter to change the settings and again to set them
	#Settings stored in a text file called 'Options.txt'
	#Write to there as well when change them
	return